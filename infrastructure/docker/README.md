# Docker Infrastructure

The root `docker-compose.yml` currently provides PostgreSQL and Redis for local development.

Production images, reverse proxy, object storage, monitoring, secrets, and backup automation must be designed before deployment.
